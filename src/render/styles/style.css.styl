@import 'nib'

a, a:link {
	cursor: pointer;
}

body {
	padding-top: 50px;
}

table {
	tfoot, tbody {
		th {
			border-top: 1px solid #DDD;
		}
	}
}

.navbar-toggle {
	margin-right: 0;
	margin-left: 15px;
	float: left;
}